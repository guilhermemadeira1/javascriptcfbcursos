"use strict";

/*Inserir "use strict" no início de um arquivo JavaScript ou no início de um bloco permite escrever códigos em modo estrito.

O modo estrito permite escrever códigos menos propensos a erros implícitos que são contornados pela linguagem e causam saídas inesperadas. Os erros antes contornados pela linguagem, passam a lançar exceções.

O modo estrito não permite, por exemplo, atribuir valor a uma variável não declarada. Em modo não estrito, o JavaScript declara implicitamente uma variável não declarada ao receber um valor.

*/
