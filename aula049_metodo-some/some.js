/*
Metodo some

<array>.some( (<elemento>, <índice>, <array>) => {
    return <filtro elementos>;
});
-> retorna true se ao menos um elemento corresponder ao filtro indicado, caso contrário, retorna false.

*/